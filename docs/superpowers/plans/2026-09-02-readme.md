# DashPi README Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish an evidence-led Korean README for DashPi with real prototype captures, GitHub-native diagrams, reproducible commands, and an honest implementation boundary.

**Architecture:** Keep the documentation self-contained at the repository root while linking deeper PRD/TRD material. Generate only three implementation-derived screenshots under `docs/assets/`; keep architecture and lifecycle visuals as maintainable Mermaid source in the README.

**Tech Stack:** GitHub Flavored Markdown, Mermaid, FastAPI static UI, Python 3.11+, pytest, Node.js/npm

## Global Constraints

- Use Korean as the primary language while preserving commands, API paths, and state names in English.
- Do not claim unimplemented Raspberry Pi hardware behavior as complete.
- Do not invent a license, institution, team identity, course number, or benchmark.
- Use actual prototype screens rather than AI-generated concept hardware.
- Add no runtime or documentation dependency.

---

### Task 1: Capture prototype evidence

**Files:**
- Create: `docs/assets/dashpi-incidents.jpg`
- Create: `docs/assets/dashpi-optical-sender.jpg`
- Create: `docs/assets/dashpi-optical-receiver.jpg`

**Interfaces:**
- Consumes: `dashpi.api.create_app`, packaged web pages, and temporary deterministic incident artifacts
- Produces: three repository-relative PNG paths consumed by `README.md`

- [x] **Step 1: Create temporary deterministic incident data**

Use existing `IncidentStore`, `atomic_write`, and `IncidentMetadata` APIs in a temporary directory. Generate a short MP4 with FFmpeg and a valid JSON/HTML report. Do not commit fixture data.

- [x] **Step 2: Start the existing local server**

Run: `PYTHONPATH=src python -m dashpi.server --data-root <temporary-data-root> --host 127.0.0.1 --port 8765`

Expected: Uvicorn serves the three current web interfaces and the incident API on port 8765.

- [x] **Step 3: Capture three real screens**

Capture the incident detail, the optical sender with a live QR frame, and the optical receiver landing state at a desktop-sized viewport. Save them to the three paths above and verify each PNG can be opened.

- [x] **Step 4: Stop the server and remove temporary data**

Stop only the process started in Step 2. Remove its dedicated temporary directory.

### Task 2: Author and verify README

**Files:**
- Create: `README.md`
- Consume: `docs/assets/dashpi-incidents.jpg`
- Consume: `docs/assets/dashpi-optical-sender.jpg`
- Consume: `docs/assets/dashpi-optical-receiver.jpg`
- Link: `docs/PRD.md`
- Link: `docs/TRD.md`

**Interfaces:**
- Consumes: current CLI flags, server flags, API routes, tests, docs, and screenshot paths
- Produces: the GitHub repository landing page

- [x] **Step 1: Run fresh baseline verification**

Run: `pytest -q`

Run: `npm test --prefix web`

Expected: both suites exit 0. Include a test count only when supported by this fresh output.

- [x] **Step 2: Write the README**

Include the approved hero, problem and stakeholder framing, differentiators, gallery, architecture diagram, lifecycle diagram, transfer comparison, status boundary, quick start, verification, safety limitations, layout, API, project docs, and external references. Use relative links and descriptive image alt text.

- [x] **Step 3: Verify documentation mechanically**

Run a local script that extracts relative Markdown links and image paths from `README.md` and asserts every referenced repository file exists. Scan for placeholders and unsupported claims. Run `git diff --check`.

Expected: all referenced local paths exist, no placeholder is present, and the diff check exits 0.

- [x] **Step 4: Review rendered structure**

Inspect `README.md` headings, code fences, and Mermaid blocks. Confirm the opening viewport communicates the problem and value, visuals have alt text, and implementation versus hardware acceptance is explicit.

- [x] **Step 5: Commit**

```bash
git add README.md docs/assets docs/superpowers/plans/2026-09-02-readme.md
git commit -m "docs: add capstone project README"
```
